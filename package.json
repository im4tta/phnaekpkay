{
  "name": "phnaek-pkay",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based satellite imagery comparison and change review tool",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "start": "vite --host 0.0.0.0",
    "check": "node -e \"const fs=require('fs'); const s=fs.readFileSync('index.html','utf8'); const a=s.indexOf('<script>')+8,b=s.indexOf('</script>',a); new Function(s.slice(a,b)); console.log('JavaScript syntax: OK')\""
  },
  "devDependencies": {
    "vite": "^7.1.3"
  },
  "dependencies": {
    "onnxruntime-web": "^1.22.0"
  }
}
